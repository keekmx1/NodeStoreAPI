// ຂໍ້ມູນ User ທີ່ຈະສ້າງ
// id, firstname, lastname, email, password, avrtar, timestamp

interface User {
  id: number;
  firstname: string;
  lastname: string;
  email: string;
  password: string;
  avatart: string;
  timestamp: number;
}

export default User;
